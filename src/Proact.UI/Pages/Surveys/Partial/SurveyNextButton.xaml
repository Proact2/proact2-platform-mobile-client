<?xml version="1.0" encoding="UTF-8" ?>
<Button
    x:Class="Proact.UI.SurveyNextButton"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:fontawesome="clr-namespace:FontAwesome"
    Margin="0,20"
    TextColor="White"
    Command="{Binding ShowNextQuesionPageCommand}"
    CornerRadius="10"
    HeightRequest="38"
    WidthRequest="65"
    Style="{x:DynamicResource FontIconButtonStyle}"
    Text="{x:Static fontawesome:FontAwesomeIcons.ArrowRight}"
    BackgroundColor="{x:DynamicResource SecondaryColor}" />